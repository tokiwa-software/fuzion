test_issue1918 =>

  count(n) =>
    true

  ignore =>
    (1..4)
      .map (_)->
        concur.Threads.env.spawn ()->
          do
            p option i32 := 1
            while !(count p.or_panic)
